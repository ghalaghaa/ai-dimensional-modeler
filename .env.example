# Frontend dev server. Points the Vite proxy at the backend API (see server/.env.example
# for the backend's own configuration — OAuth credentials, encryption key, etc).
VITE_API_TARGET=http://localhost:5001
